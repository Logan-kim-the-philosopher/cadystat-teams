export type TicketRecord = {
  id: string;
  title: string;
  team: string;
  teamSlug: string;
  status: string;
  priority: string;
  type: string;
  reporter: string;
  createdAt: string;
  summary: string;
};
